## What possible steps will a installation involves?

1. upload files
2. execute commands with information provided.

Every execute will accept three parameters which are three file names, clusterInfo, selfInfo, customInfo. 